import React from 'react';

import { VirtualScroll } from './VirtualScroll';

export default {
  title: 'VirtualScroll',
  component: VirtualScroll,
};

export const Default = () => <VirtualScroll />;
